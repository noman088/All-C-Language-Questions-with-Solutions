#include <iostream>
using namespace std;

int main() {
    int a;
    cin >> a; // Take the input from the user
    int dummy; // Declare a dummy variable
    dummy = a; // Store the input in the dummy variable (but don't use it)
    cout << "Hello, world!";
    return 0;
}